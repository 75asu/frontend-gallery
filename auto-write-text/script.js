const text = "Nothing important, dropped this message here to waste few seconds of your life while you are reading it       ...";

let index = 0;

function writeText() {
    document.body.innerText = text.slice(0, index);

    index++;

    if (index > text.length) {
        index = 0;
    }
}

setInterval(writeText, 100);